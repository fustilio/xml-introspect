{
  "name": "@xml-introspect/root",
  "version": "0.2.0",
  "private": true,
  "description": "A powerful CLI tool and library for analyzing large XML files and generating representative samples with identical structure",
  "keywords": [
    "xml",
    "xsd",
    "introspect",
    "analysis",
    "sampling",
    "wordnet",
    "lmf",
    "cli",
    "typescript",
    "data-processing"
  ],
  "author": "fustilio",
  "license": "MIT",
  "homepage": "https://github.com/fustilio/xml-introspect#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/fustilio/xml-introspect.git"
  },
  "bugs": {
    "url": "https://github.com/fustilio/xml-introspect/issues"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm --filter ./packages/* build",
    "test": "pnpm --filter ./packages/* test",
    "dev": "pnpm --filter ./packages/* dev",
    "clean": "pnpm --filter ./packages/* clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "lint-staged": {
    "*.{ts,js}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6",
    "cross-env": "^7.0.3",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.8"
  },
  "packageManager": "pnpm@9.5.0",
  "workspaces": [
    "packages/*"
  ]
}
